#include <stdio.h>
#include <vector>

int main() {
	int T;
	int aux=0;
	char a, b;
	vector<char> algo;
	vector<algo> cards(52);

	scanf("%d", &T);

	while (T--){
		aux++;

		for (int i=0; i<52; i++) {
			scanf("%c%c ", &cards[i][0], &cards[i][1]);
		}



		printf("Case %d: %c%c", aux, )
	}

	return 0;
}